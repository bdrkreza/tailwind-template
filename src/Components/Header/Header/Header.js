import React from 'react';
import HeroSection from '../../HeroSection/HeroSection';

const Header = () => {
    return (
        <div>
            <HeroSection />
        </div>
    );
};

export default Header;